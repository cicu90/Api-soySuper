const { isTag } = require("domhandler");
const crawler = require("../src/crawler/crawler");

describe("the crawler", () => {

    let testing;

    test("equal user", () => {
        expect(testing).toBe()
    });

})